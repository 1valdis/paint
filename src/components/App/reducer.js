import reducer from '../Colors/reducer'

const appReducer = (
  state = {
    imageData: null,
    colors: {
      primaryColor: null,
      secondaryColor: null,
      list: []
    }
  },
  action
) => {
  return state
}

export default appReducer
